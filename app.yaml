name: h2ogpt-chatbot
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/h2ogpt-chatbot
  original_url: https://huggingface.co/spaces/Chris4K/h2ogpt-chatbot

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"

# Updated: 2026-01-03 14:07:22
